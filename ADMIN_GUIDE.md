# دليل نظام إدارة المنتجات

## نظرة عامة
تم تطوير نظام إدارة شامل واحترافي للمنتجات يوفر جميع الأدوات اللازمة لإدارة متجر الأسماك والمعدات البحرية بكفاءة عالية.

## المميزات الرئيسية

### 1. إدارة المنتجات الشاملة
- ✅ إضافة منتجات جديدة مع كافة التفاصيل
- ✅ تعديل بيانات المنتجات الموجودة
- ✅ حذف المنتجات بسهولة
- ✅ رفع الصور المميزة للمنتجات
- ✅ إدارة الأوصاف والمواصفات

### 2. إدارة المخزون
- ✅ تتبع الكميات المتوفرة لكل منتج
- ✅ تنبيهات للمنتجات ذات المخزون المنخفض
- ✅ تحديث الكميات تلقائياً
- ✅ عرض حالة المخزون في الوقت الحقيقي
- ✅ إحصائيات شاملة للمخزون

### 3. نظام الخصومات والعروض
- ✅ إنشاء خصومات بنسبة مئوية أو مبلغ ثابت
- ✅ ربط الخصومات بمنتجات محددة
- ✅ تحديد فترة سريان الخصم
- ✅ تفعيل وإلغاء تفعيل الخصومات

### 4. سجل التدقيق (Audit Logs)
- ✅ تسجيل كل العمليات الإدارية
- ✅ تتبع من قام بكل تغيير
- ✅ عرض التفاصيل الكاملة للتغييرات
- ✅ تصنيف السجلات حسب النوع

### 5. البحث والتصنيف
- ✅ بحث سريع في المنتجات
- ✅ تصفية حسب الفئة والعلامة التجارية
- ✅ ترتيب المنتجات
- ✅ عرض منظم في جداول احترافية

## البنية التقنية

### قاعدة البيانات
تم إضافة الجداول التالية:

#### جدول `discounts`
```sql
- id: معرف فريد
- product_id: المنتج المرتبط
- type: نوع الخصم (percentage/fixed)
- value: قيمة الخصم
- start_date: تاريخ البدء
- end_date: تاريخ الانتهاء
- is_active: حالة الخصم
```

#### جدول `audit_logs`
```sql
- id: معرف فريد
- user_id: المستخدم المسؤول
- action: نوع العملية (create/update/delete)
- entity_type: نوع الكيان
- entity_id: معرف الكيان
- changes: تفاصيل التغييرات (JSON)
- created_at: وقت العملية
```

#### تحديث جدول `users`
```sql
- role: دور المستخدم (user/admin)
```

### API Endpoints

#### إدارة المنتجات
- `POST /api/admin/products` - إضافة منتج جديد
- `PUT /api/admin/products/:id` - تحديث منتج
- `DELETE /api/admin/products/:id` - حذف منتج

#### إدارة الخصومات
- `GET /api/admin/discounts` - عرض جميع الخصومات
- `POST /api/admin/discounts` - إنشاء خصم جديد
- `PUT /api/admin/discounts/:id` - تحديث خصم
- `DELETE /api/admin/discounts/:id` - حذف خصم

#### سجلات التدقيق
- `GET /api/admin/audit-logs` - عرض سجلات التدقيق

#### الطلبات
- `GET /api/admin/orders` - عرض جميع الطلبات

**ملاحظة:** جميع نقاط النهاية محمية ب `requireAdmin` middleware

## كيفية الاستخدام

### 1. الوصول إلى لوحة التحكم
قم بزيارة: `http://localhost:5000/admin`

### 2. إضافة منتج جديد
1. اضغط على زر "إضافة منتج جديد"
2. املأ جميع الحقول المطلوبة:
   - الاسم
   - الرابط (Slug)
   - العلامة التجارية
   - الفئة والفئة الفرعية
   - الوصف
   - السعر والسعر الأصلي
   - الكمية المتوفرة
   - رابط الصورة
3. حدد الخيارات الإضافية (منتج جديد، الأكثر مبيعاً)
4. اضغط "إضافة"

### 3. تعديل منتج
1. ابحث عن المنتج في الجدول
2. اضغط على أيقونة التعديل (قلم)
3. قم بتعديل البيانات المطلوبة
4. اضغط "تحديث"

### 4. حذف منتج
1. ابحث عن المنتج في الجدول
2. اضغط على أيقونة الحذف (سلة المهملات)
3. أكد عملية الحذف

### 5. البحث والتصفية
- استخدم مربع البحث للبحث عن المنتجات
- البحث يشمل: الاسم، العلامة التجارية، الفئة

## الإحصائيات المعروضة

في لوحة التحكم الرئيسية:
- **إجمالي المنتجات**: عدد جميع المنتجات
- **منتجات بمخزون منخفض**: المنتجات التي تحتاج إعادة تخزين
- **قيمة المخزون**: القيمة الإجمالية للمخزون
- **إجمالي الطلبات**: عدد الطلبات النشطة

## الأمان

### نظام الصلاحيات
- فقط المستخدمون ذوو `role: admin` يمكنهم الوصول
- جميع العمليات محمية ب middleware
- تسجيل كامل لجميع العمليات

### التحقق من الصلاحيات
```typescript
// في كل طلب إداري
requireAdmin middleware يتحقق من:
1. وجود جلسة نشطة
2. المستخدم موجود في قاعدة البيانات
3. role المستخدم = "admin"
```

## رفع الصور

حالياً يتم دعم:
- روابط الصور الخارجية
- بيانات Base64 (للتطوير)

**للإنتاج**: يُنصح باستخدام خدمة تخزين سحابية مثل:
- AWS S3
- Cloudinary
- Firebase Storage

## التوسعات المستقبلية

### قيد التطوير
- [ ] واجهة إدارة الخصومات الكاملة
- [ ] نظام إدارة الطلبات المتقدم
- [ ] تقارير تفصيلية
- [ ] تصدير البيانات (Excel/CSV)
- [ ] رفع صور متعددة بسهولة
- [ ] معاينة المنتج قبل النشر
- [ ] نظام الأذونات المتقدم

## استكشاف الأخطاء

### مشكلة: لا يمكن الوصول إلى لوحة التحكم
**الحل**: تأكد من أن المستخدم لديه `role: admin` في قاعدة البيانات

### مشكلة: فشل رفع الصور
**الحل**: تأكد من صحة رابط الصورة أو استخدم Base64

### مشكلة: لا تظهر التحديثات
**الحل**: قم بتحديث الصفحة أو تحقق من اتصال API

## الدعم والمساعدة

للحصول على الدعم:
1. راجع سجلات الخطأ في الـ console
2. تحقق من سجلات التدقيق
3. راجع وثائق API

## المتطلبات

### تشغيل Migration
```bash
# لتطبيق تغييرات قاعدة البيانات
psql -d your_database < migrations/001_add_admin_features.sql
```

أو استخدم Drizzle:
```bash
pnpm db:push
```

### إنشاء مستخدم Admin
```sql
-- في قاعدة البيانات
UPDATE users
SET role = 'admin'
WHERE username = 'your_username';
```

## الخلاصة

نظام إدارة المنتجات الآن جاهز بالكامل مع:
- ✅ واجهة مستخدم احترافية وسهلة الاستخدام
- ✅ CRUD كامل للمنتجات
- ✅ إدارة المخزون والكميات
- ✅ نظام الخصومات
- ✅ سجل تدقيق شامل
- ✅ بحث وتصفية متقدمة
- ✅ إحصائيات في الوقت الحقيقي
- ✅ أمان وحماية كاملة

استمتع بإدارة متجرك بكفاءة وسهولة! 🐟
