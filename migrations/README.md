# Database Migrations

This directory contains database migration files generated by Drizzle Kit.

## Generate migrations

```bash
pnpm db:push
```

## Apply migrations

Migrations are applied automatically when using `db:push`.

For production deployments, consider using proper migration workflow with Drizzle Kit.

