# دليل البدء السريع - نظام إدارة المنتجات

## 🚀 التثبيت والإعداد

### 1. تطبيق تغييرات قاعدة البيانات

إذا كنت تستخدم PostgreSQL:
```bash
psql -d your_database_name < migrations/001_add_admin_features.sql
```

أو باستخدام Drizzle:
```bash
pnpm db:push
```

### 2. إنشاء مستخدم مسؤول

قم بتشغيل هذا الأمر في قاعدة البيانات:
```sql
-- أنشئ مستخدم جديد (إذا لم يكن موجوداً)
INSERT INTO users (username, password, email, role)
VALUES ('admin', 'hashed_password_here', 'admin@example.com', 'admin');

-- أو حدّث مستخدم موجود ليصبح مسؤولاً
UPDATE users
SET role = 'admin'
WHERE username = 'your_username';
```

### 3. تشغيل الخادم

```bash
# تشغيل في وضع التطوير
pnpm dev

# أو في وضع الإنتاج
pnpm build
pnpm start
```

### 4. الوصول إلى لوحة التحكم

افتح المتصفح وانتقل إلى:
```
http://localhost:5000/admin
```

## 📋 العمليات الأساسية

### إضافة منتج جديد

1. اضغط "إضافة منتج جديد"
2. املأ النموذج:
   ```
   - الاسم: اسم المنتج بالعربية
   - Slug: product-name-in-english
   - العلامة التجارية: مثل Marina, Aqueon
   - الفئة: مثل أسماك، معدات، طعام
   - الفئة الفرعية: تصنيف فرعي
   - الوصف: وصف تفصيلي للمنتج
   - السعر: بالدينار العراقي
   - الكمية المتوفرة: العدد في المخزون
   ```
3. أضف صورة (رابط URL)
4. حدد الخيارات (منتج جديد، الأكثر مبيعاً)
5. اضغط "إضافة"

### تعديل منتج

1. ابحث عن المنتج في الجدول
2. اضغط أيقونة القلم ✏️
3. عدّل البيانات
4. اضغط "تحديث"

### حذف منتج

1. ابحث عن المنتج
2. اضغط أيقونة سلة المهملات 🗑️
3. أكد الحذف

## 🔐 الأمان

- فقط المستخدمون الذين `role = admin` يمكنهم الوصول
- جميع العمليات محمية بـ middleware
- يتم تسجيل جميع التغييرات في audit_logs

## 📊 المميزات المتاحة

✅ **الآن**:
- إدارة كاملة للمنتجات (إضافة، تعديل، حذف)
- إدارة المخزون والكميات
- بحث وتصفية المنتجات
- إحصائيات في الوقت الحقيقي
- سجل التدقيق الكامل

🔜 **قريباً**:
- واجهة إدارة الخصومات الكاملة
- نظام الطلبات المتقدم
- التقارير والإحصائيات
- تصدير البيانات

## 🐛 استكشاف المشاكل

### المشكلة: لا يمكنني الوصول إلى /admin

**الحل**:
1. تحقق من أن المستخدم لديه `role: admin`
2. تأكد من تسجيل الدخول
3. افحص console للأخطاء

### المشكلة: الصور لا تظهر

**الحل**:
1. تأكد من صحة رابط الصورة
2. تحقق من أن الصورة متاحة عبر الإنترنت
3. جرب صورة أخرى

### المشكلة: لا تحفظ التغييرات

**الحل**:
1. افحص console للأخطاء في API
2. تأكد من أن قاعدة البيانات متصلة
3. راجع سجل الخادم

## 📞 الدعم

للمساعدة:
1. راجع `ADMIN_GUIDE.md` للتفاصيل الكاملة
2. افحص سجلات التدقيق
3. راجع documentation API

---

استمتع بإدارة منتجاتك بسهولة! 🎉
