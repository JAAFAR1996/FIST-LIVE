# 🔬 أسئلة التدقيق المتقدمة لـ FIST-LIVE

## مجموعة الأسئلة الاحترافية لضمان الجودة

### 1️⃣ أسئلة الوظائف الأساسية

#### هل جميع المنتجات تحميل بشكل صحيح؟
```
اختبر:
✓ الصفحة الرئيسية تعرض جميع المنتجات
✓ لا توجد منتجات مفقودة
✓ الأسعار متطابقة مع قاعدة البيانات
✓ الصور تحميل بسرعة (< 1 ثانية لكل صورة)
✓ الصور ذات جودة عالية (لا ضبابية)
```

#### هل البحث يعمل بشكل دقيق؟
```
اختبر:
✓ البحث بـ "filter" يعطي النتائج الصحيحة
✓ البحث بـ "حوض" يعرض الأحواض فقط
✓ البحث حساس لحالة الأحرف (case-insensitive)
✓ البحث بـ أحرف عربية يعمل بشكل صحيح
✓ لا توجد نتائج لـ "zzzzzzz" مع رسالة واضحة
✓ وقت البحث < 500ms حتى مع آلاف المنتجات
```

#### هل عربة التسوق آمنة؟
```
اختبر:
✓ الكمية المضافة لا تتجاوز الكمية المتاحة
✓ الكمية لا تنخفض عن 1
✓ حذف منتج يزيله نهائياً
✓ الأسعار تُحسب بشكل دقيق
✓ تحديث الكمية يُحدث السعر فوراً
✓ العربة تحتفظ ببيانات حتى بعد الإغلاق (localStorage)
✓ مسح ملفات التخزين المؤقت لا يمسح العربة
```

---

### 2️⃣ أسئلة المستخدمين والحسابات

#### هل التسجيل الجديد آمن وسهل؟
```
اختبر:
✓ لا يمكن التسجيل برسالة بريد موجودة بالفعل
✓ كلمة المرور يجب أن تحتوي على:
  - حرف كبير (A-Z)
  - حرف صغير (a-z)
  - رقم (0-9)
  - رمز خاص (!@#$%^&*)
✓ رسالة تأكيد البريد تصل خلال 5 دقائق
✓ لا يمكن الدخول قبل تأكيد البريد
✓ رابط التأكيد ينتهي بعد ساعة واحدة
✓ إمكانية إعادة إرسال رسالة التأكيد
```

#### هل تسجيل الدخول يعمل بدقة؟
```
اختبر:
✓ تسجيل الخروج ثم الدخول مجدداً يعمل
✓ تسجيل دخول من نافذتين مختلفتين
✓ الجلسة تنتهي بعد 30 دقيقة من عدم النشاط
✓ لا يمكن الدخول بكلمة مرور خاطئة
✓ عدد محاولات خاطئة محدود (5 محاولات ثم حظر مؤقت)
✓ نسيان كلمة المرور يرسل رابط آمن
✓ رابط تعيين كلمة المرور ينتهي بعد ساعة واحدة
```

#### هل ملف المستخدم يعرض البيانات الصحيحة؟
```
اختبر:
✓ جميع البيانات المحفوظة تظهر بشكل صحيح
✓ تعديل البيانات يُحدث فوراً
✓ لا يمكن تعديل بريد ليوجد بالفعل
✓ سجل الطلبات يعرض جميع الطلبات السابقة
✓ العناوين المحفوظة تظهر وتُستخدم
✓ حذف حساب ينقل البيانات للأرشيف (لا يحذف)
```

---

### 3️⃣ أسئلة الطلبات والفواتير

#### هل عملية الشراء سلسة وآمنة؟
```
اختبر:
✓ خطوات الشراء واضحة (4-5 خطوات فقط)
✓ يمكن حفظ تقدم الشراء والعودة لاحقاً
✓ معلومات الشحن تُحفظ للاستخدام المستقبلي
✓ يمكن اختيار عنوان محفوظ سابقاً
✓ طرق الدفع تعرض بوضوح
✓ خطأ في الدفع يعيد المحاولة بدون فقد البيانات
✓ لا يمكن إرسال الطلب بدون ملء البيانات الإلزامية
```

#### هل الفاتورة صحيحة وموثوقة؟
```
اختبر الفاتورة تتضمن:
✓ رقم الفاتورة فريد
✓ تاريخ الفاتورة صحيح
✓ بيانات المشتري صحيحة
✓ بيانات الشحن صحيحة
✓ المنتجات مع الأسعار الصحيحة
✓ الكميات صحيحة
✓ المجموع الجزئي = مجموع الأسعار
✓ الضرائب محسوبة بشكل صحيح
✓ رسوم الشحن صحيحة
✓ المجموع النهائي صحيح
✓ يمكن طباعة الفاتورة
✓ يمكن تحميل الفاتورة بصيغة PDF
✓ الفاتورة تحتوي على رقم هاتف الدعم
✓ الفاتورة واضحة وقابلة للقراءة
```

#### هل الطلبات تُحفظ بشكل صحيح؟
```
اختبر:
✓ كل طلب له رقم فريد
✓ الطلب يظهر في ملف المستخدم فوراً
✓ البريد الإلكتروني يصل بالتأكيد
✓ حالة الطلب تتحدث تلقائياً
✓ يمكن تتبع حالة الطلب
✓ الطلب يظهر في لوحة الإدارة
✓ لا يمكن تعديل الطلب بعد تأكيده (إلا من الإدارة)
✓ لا يمكن حذف الطلب (يُؤرشف فقط)
```

---

### 4️⃣ أسئلة قاعدة البيانات والربط

#### هل البيانات تُحفظ بشكل آمن؟
```
اختبر:
✓ كل بيان منتج محفوظ بدقة
✓ لا توجد نسخ مكررة من نفس المنتج
✓ حذف منتج لا يحذف الطلبات المرتبطة
✓ تعديل منتج لا يؤثر على الطلبات السابقة
✓ الكمية المتاحة تنخفض عند كل طلب
✓ الكمية لا تصبح سالبة
✓ استعادة الطلب تعيد الكمية
```

#### هل العلاقات بين الجداول صحيحة؟
```
اختبر الربط بين:
✓ user → orders (1:N)
✓ order → order_items (1:N)
✓ order_item → product (N:1)
✓ product → category (N:1)
✓ product → images (1:N)
✓ product → reviews (1:N)
✓ review → user (N:1)

لا يوجد يتيم (بيانات بدون والد):
✓ لا توجد order_items بدون order
✓ لا توجد reviews بدون product
✓ لا توجد images بدون product
```

#### هل الاستعلامات سريعة؟
```
اختبر:
✓ جلب 100 منتج < 200ms
✓ البحث عن منتج < 300ms
✓ جلب الطلبات < 500ms
✓ التصفية بعدة معايير < 400ms
✓ إضافة منتج < 100ms
✓ تعديل منتج < 100ms
✓ حذف منتج < 100ms
✓ لا توجد استعلامات N+1
```

---

### 5️⃣ أسئلة الأداء والسرعة

#### هل الموقع سريع الاستجابة؟
```
اختبر:
✓ التفاعل مع الأزرار < 100ms
✓ الانتقال بين الصفحات < 300ms
✓ تحديث الصفحة < 1 ثانية
✓ البحث المباشر (Real-time Search) بلا تأخير
✓ تحديث عربة التسوق < 200ms
✓ فتح صفحة المنتج < 500ms
```

#### هل الصور محسّنة؟
```
اختبر:
✓ حجم الصورة < 100KB
✓ دقة الصورة مناسبة (لا ضبابية)
✓ صور مختلفة الأحجام تُعرض حسب الجهاز
✓ الصور تُحمّل بالتوازي (لا واحدة تلو الأخرى)
✓ صور مخبئة تُحمّل عند الحاجة (Lazy Loading)
✓ استخدام صيغة WebP (أسرع من JPG)
```

#### هل استهلاك البيانات معقول؟
```
اختبر على 3G/LTE:
✓ الصفحة الرئيسية < 1MB
✓ صفحة المنتج < 500KB
✓ لوحة الإدارة < 1MB
✓ البيانات تُضغط (gzip)
✓ ملفات CSS و JS مُعرّفة (minified)
✓ عدم إرسال بيانات غير ضرورية
```

---

### 6️⃣ أسئلة الأمان والحماية

#### هل الموقع محمي من الهجمات؟
```
اختبر:
SQL Injection:
✓ جرّب: ' OR '1'='1
✓ جرّب: '; DROP TABLE users; --
✓ جرّب: admin' --
✗ لا يجب أن تعمل أي من هذه

XSS:
✓ جرّب: <script>alert('XSS')</script>
✓ جرّب: <img src=x onerror="alert('XSS')">
✓ جرّب: javascript:alert('XSS')
✗ لا يجب تنفيذ السكريبتات

CSRF:
✓ تحقق من وجود CSRF token في النماذج
✓ لا يمكن إرسال form بدون token
✓ Token يتغير كل عملية
```

#### هل البيانات الحساسة محمية؟
```
اختبر:
✓ كلمات المرور مشفّرة (bcrypt/argon2)
✓ كلمات المرور لا تُرسل نصية (HTTPS فقط)
✓ بيانات الدفع لا تُحفظ (استخدم Stripe/PayPal)
✓ رموز الجلسة آمنة (httpOnly, secure, sameSite)
✓ الرموز تنتهي بعد وقت معين
✓ تسجيل الخروج يحذف الرمز
```

#### هل الصلاحيات صحيحة؟
```
اختبر:
بدون دخول:
✓ لا تصل لـ /admin
✓ لا تصل لـ API محمي
✓ تُعاد للصفحة الرئيسية

بحساب عادي:
✓ لا تصل لـ /admin
✓ لا تستطيع حذف منتج
✓ لا تستطيع تعديل طلب الآخرين
✓ تُرى فقط طلباتك وبيانات حسابك

بحساب إدارة:
✓ تصل لـ /admin
✓ تستطيع تعديل أي منتج
✓ تستطيع تعديل أي طلب
✓ ترى جميع المستخدمين
```

---

### 7️⃣ أسئلة التصميم والتجربة

#### هل الموقع متجاوب على جميع الأجهزة؟
```
الهواتف الصغيرة (320px):
✓ عرض الموقع صحيح
✓ يمكن قراءة النص
✓ الأزرار قابلة للضغط (minimum 44px)
✓ لا توجد أشرطة تمرير أفقية

الهواتف الكبيرة (375px-480px):
✓ جميع العناصر ظاهرة
✓ التخطيط متسق
✓ الصور توازن مع النص

الأجهزة اللوحية (768px):
✓ استخدام العمودين/الأعمدة
✓ الكفاءة أفضل
✓ العرض أوسع

الحواسيب (1920px+):
✓ استخدام مساحة الشاشة بحكمة
✓ عدم وجود نصوص طويلة جداً
✓ ترتيب عناصر متوازن
```

#### هل الألوان والتباين جيدة؟
```
اختبر:
✓ النسبة بين الخط والخلفية ≥ 4.5:1
✓ الأزرار والعناصر التفاعلية واضحة
✓ الألوان متسقة في الموقع كله
✓ لا تعتمد على اللون وحده (استخدم رموز أيضاً)
✓ اختبر بعمى الألوان (DevTools > Rendering > Emulate CSS media feature prefers-color-scheme)
```

#### هل الخطوط قابلة للقراءة؟
```
اختبر:
✓ حجم الخط ≥ 16px للنصوص الأساسية
✓ ارتفاع السطر ≥ 1.5
✓ عرض السطر ≤ 80 حرف
✓ الخط واضح وسهل القراءة
✓ البخط العربية صحيحة (اختبر: ـــ ـ ـــ)
✓ اختبر النسخ (Copy-Paste يعمل بشكل صحيح)
```

---

### 8️⃣ أسئلة التدويل والعولمة

#### هل الموقع يدعم اللغة العربية بشكل كامل؟
```
اختبر:
✓ جميع النصوص باللغة العربية (إن كان الموقع عربياً)
✓ النصوص الطويلة تُقسم بشكل صحيح
✓ الأرقام الهندية/العربية تُعرض بشكل صحيح
✓ التواريخ بصيغة صحيحة (DD/MM/YYYY أو similar)
✓ الرموز النقدية صحيحة (دينار/ريال/إلخ)
✓ الاتجاه RTL يعمل بشكل صحيح
```

#### هل الموقع يعمل في دول مختلفة؟
```
اختبر:
✓ الأسعار تتحدث حسب البلد (إن لزم)
✓ طرق الدفع متاحة لكل بلد
✓ الشحن يعمل لدول مختلفة
✓ الضرائب محسوبة حسب البلد
✓ دعم عملات مختلفة (إن لزم)
```

---

### 9️⃣ أسئلة الامتثال القانوني

#### هل الموقع متطابق مع القوانين؟
```
✓ توجد سياسة خصوصية واضحة
✓ توجد شروط الاستخدام
✓ توجد سياسة الاسترجاع والاستبدال
✓ سياسة الكوكيز واضحة
✓ معلومات الاتصال متوفرة
✓ رابط عدم الاشتراك في البريد الإلكتروني يعمل
✓ توافق مع GDPR (حذف البيانات)
```

---

### 🔟 أسئلة الصيانة والدعم

#### هل التسجيلات كاملة؟
```
اختبر:
✓ وجود logs لكل عملية مهمة
✓ logs تتضمن:
  - الوقت والتاريخ
  - المستخدم (إن وجد)
  - العملية (ماذا حدث)
  - النتيجة (نجاح/فشل)
  - التفاصيل الإضافية
✓ Logs محفوظة لمدة 30 يوم على الأقل
✓ يمكن البحث في Logs
```

#### هل الأخطاء موثقة؟
```
اختبر:
✓ أي خطأ في السيرفر يُسجل مع stack trace
✓ الأخطاء تُرسل للإدارة (email/monitoring)
✓ رسائل الأخطاء للمستخدمين واضحة (ليست تقنية)
✓ الأخطاء الشائعة لها حل موثق
```

#### هل هناك طريقة للتواصل والدعم؟
```
✓ رقم هاتف/بريد إلكتروني للدعم ظاهر
✓ نموذج اتصال يعمل بشكل صحيح
✓ الرسائل تصل للدعم بنجاح
✓ استجابة الدعم في وقت معقول
```

---

## 📊 مثال على ملخص الاختبار

```markdown
# ملخص اختبار FIST-LIVE
التاريخ: 10/12/2025
المختص: أنت
المدة: 4 ساعات

## النتائج الإجمالية
- ✓ 95% من الاختبارات نجحت
- ⚠️ 3 مشاكل بسيطة
- ✗ مشكلة واحدة متوسطة

## المشاكل المكتشفة

### 1. حساب الضرائب خاطئ (متوسط)
- **الوصف**: الضريبة تُحسب بشكل خاطئ (10% بدلاً من 15%)
- **التأثير**: الفاتورة تظهر سعر أقل من الواقع
- **الحل**: تحديث دالة الضريبة في server/routes/orders.ts
- **الأولوية**: عالية جداً
- **الحالة**: قيد الإصلاح

### 2. الصور تحميل بطيئة (بسيط)
- **الوصف**: صور المنتجات تأخذ وقت طويل لتحميل
- **التأثير**: تجربة المستخدم أقل سلاسة
- **الحل**: استخدام WebP + Lazy Loading
- **الأولوية**: متوسطة
- **الحالة**: مخطط

### 3. رسالة خطأ البحث غير واضحة (بسيط)
- **الوصف**: "No results" بدلاً من رسالة واضحة
- **التأثير**: قد يربك المستخدم
- **الحل**: تحديث الرسالة إلى "لم نجد منتجات تطابق بحثك"
- **الأولوية**: منخفضة
- **الحالة**: مخطط

## التوصيات
1. تحسين سرعة الصور (استخدام CDN)
2. إضافة unit tests للعمليات الحسابية
3. مراقبة الأداء على الإنتاج (Sentry/LogRocket)

## الخلاصة
الموقع جاهز للإطلاق مع تصحيح المشكلة الأساسية (حساب الضرائب).
```

---

**بعد الإجابة على جميع هذه الأسئلة، يمكنك أن تثق تماماً أن موقعك جاهز للإطلاق! ✨**
